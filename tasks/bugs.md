erase i bug when i say it's fixed.

- on mobile, the drag-mark functionality doesn't work the same. It's supposed to also flip the inital starting box that you mouse-down on. it works on desktop browser but not with my thumb. only does the subsequent cells.